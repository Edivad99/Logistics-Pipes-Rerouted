/** Messages about a pipe itself: its own state, and what the player sets on it. Sent client to server. */
@NullMarked
package logisticspipes.network.to_server.pipe;

import org.jspecify.annotations.NullMarked;
