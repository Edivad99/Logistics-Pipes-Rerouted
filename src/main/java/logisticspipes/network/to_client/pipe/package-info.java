/** Messages about a pipe itself: its own state, and what the player sets on it. Sent server to client. */
@NullMarked
package logisticspipes.network.to_client.pipe;

import org.jspecify.annotations.NullMarked;
